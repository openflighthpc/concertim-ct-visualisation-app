<%
  set_title "Usage limits for team #{@team.name}"
-%>

<h2 class="text-center">
  Cloud Platform Limits<a href="#"
                          title="Allowed usage for the team, as determined by your cloud platform configuration. Please note this does not account for overall available resources on your cloud platform. "
                          onClick="return false;"><sup>?</sup></a>
</h2>

<%= render partial: 'statistics/stats', locals: { stats: {totals: presenter_for(@limits, LimitsPresenter).grouped_limits } } %>
